#Files for installation
INSTALL=( check_asterisk.py )

#Installed name
INSTALLAS=( check_asterisk )

if [ ${#INSTALL[@]} -ne ${#INSTALLAS[@]} ]; then
    echo "Install files and install as counts do not match! Installer is broken."
    exit 1
fi